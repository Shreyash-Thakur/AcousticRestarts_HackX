const { expect } = require("chai");

describe("InvoToken", () => {
  it("should mint an invoice token after valid IRN verification");
  it("should revert minting if IRN is already tokenized");
  it("should deduct origination fee on full funding");
});
